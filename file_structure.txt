teamchat-backend/
├── .github/                    # CI/CD workflows (GitHub Actions, linting, testing)
├── cmd/                        # Main entry points for your binaries
│   ├── api-server/             # Decoupled HTTP API for processing message Ingress (POST requests)
│   │   └── main.go
│   └── ws-worker/              # Stateful WebSocket cluster servers for streaming to clients
│       └── main.go
├── config/                     # Configuration files (JSON, YAML, or environment matrices)
│   └── config.yaml
├── deployments/                # Infrastructure configurations
│   ├── docker-compose.yml      # Local development setup (MySQL/Mongo, Redis)
│   └── k8s/                    # Kubernetes manifests for scaling stateless/stateful pods
├── internal/                   # Private application code (cannot be imported by external projects)
│   ├── config/                 # Go structures mapping configuration files
│   ├── domain/                 # Core domain models / business entities (Polymorphic Conversation, Message)
│   │   ├── conversation.go
│   │   ├── message.go
│   │   └── user.go
│   ├── handler/                # Presentation layer (HTTP Controllers & WebSocket upgrading)
│   │   ├── http/
│   │   │   ├── middleware/     # Auth, Rate Limiting, Idempotency checking
│   │   │   └── message_handler.go
│   │   └── ws/
│   │       ├── client.go       # Tracks active persistent ws.Conn
│   │       ├── hub.go          # Manages local room registration
│   │       └── ws_handler.go   # Upgrades connection and handles Read/Write loops
│   ├── repository/             # Data access layer (Implements Database Interfaces)
│   │   ├── db.go               # Client initialization (SQL driver / Mongo driver)
│   │   ├── msg_repo.go         # Write-Ahead log persistence and delta sync queries
│   │   └── cache_repo.go       # Distributed Redis lookups for presence & idempotency tokens
│   ├── service/                # Business Logic orchestration layer
│   │   ├── message_service.go  # Validates sender -> Generates Snowflake ID -> Saves to Repo -> Publishes to Broker
│   │   └── sync_service.go     # Computes delta missing messages for Catch-Up protocol
│   └── pubsub/                 # Decoupled Message Broker Wrapper
│       ├── broker.go           # Universal Broker Interface
│       └── redis_pubsub.go     # Concrete Redis Cluster Pub/Sub implementation
├── pkg/                        # Public library utilities (safe for external tool ingestion)
│   └── snowflake/              # Custom distributed unique monotonically increasing ID generator
├── scripts/                    # Shell scripts for database migrations or stress testing
│   └── migrations/             # Schema evolutionary files (.sql or migration binaries)
├── go.mod                      # Module definition
├── go.sum                      # Checksums for dependencies
└── Makefile                    # Standard engineering build, run, and test targets